const string = process.argv[2];
console.log(string);

const values = [];

let min = 0;

for (let max = 0; min < max.length; i++) {
    while




// PSEUDO CODE :
// IN values[] of NUMBER
// LET min = 0
// LET max = values LENGTH - 1
// // Itérer tant que min < max
// WHILE min < max
//     LET actual = values[max] // max = 4, value = 5
// values[max] = values[min]
// values[min] = actual
// min++
// max--
// OUT values[] // [5, 4, 3, 2, 1]